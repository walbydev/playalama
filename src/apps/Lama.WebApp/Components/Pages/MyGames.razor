@page "/my-games"
@layout MainLayout
@implements IDisposable
@inject AuthService Auth
@inject LamaApiClient Api
@inject NavigationManager Navigation

<PageTitle>Mes parties — Playalama</PageTitle>

@if (!Auth.IsInitialized)
{
    <div class="auth-page">
        <div class="auth-card text-center">
            <h1>Mes parties</h1>
            <p class="text-muted mt-2" aria-live="polite">Chargement de votre session…</p>
        </div>
    </div>
}
else if (!Auth.IsLoggedIn)
{
    <div class="auth-page">
        <div class="auth-card text-center">
            <h1>Mes parties</h1>
            <p class="text-muted mt-2">Vous devez être connecté pour voir votre historique.</p>
            <a href="/login" class="btn btn-primary mt-3">Se connecter</a>
        </div>
    </div>
}
else
{
    <div class="page-hero">
        <div class="eyebrow">Historique</div>
        <div class="flex items-center justify-between flex-wrap gap-2">
            <h1>Mes parties</h1>
            <button class="btn btn-ghost btn-sm" type="button" @onclick="LoadAsync" disabled="@_busy">Rafraîchir</button>
        </div>
    </div>

    <div class="section">
        <div class="container">

            @if (!string.IsNullOrWhiteSpace(_error))
            {
                <p class="form-error mb-3" role="alert">@_error</p>
            }

            @if (_busy)
            {
                <p class="text-muted text-center" aria-live="polite">Chargement…</p>
            }
            else if (_games.Count == 0)
            {
                <div class="empty-state">
                    <span class="empty-state-icon" aria-hidden="true">🎮</span>
                    <p>Aucune partie pour le moment.</p>
                    <a href="/games" class="btn btn-primary mt-2">Rejoindre une partie</a>
                </div>
            }
            else
            {
                <div class="flex gap-3 mb-4 flex-wrap">
                    <div class="card" style="text-align:center; min-width:120px">
                        <div style="font-size:1.5rem; font-weight:900; color:var(--accent2)">@_games.Count</div>
                        <div class="text-dim" style="font-size:.8rem; text-transform:uppercase; letter-spacing:1px">Parties</div>
                    </div>
                    <div class="card" style="text-align:center; min-width:120px">
                        <div style="font-size:1.5rem; font-weight:900; color:var(--accent2)">@_games.Count(g => IsActiveStatus(g.Status))</div>
                        <div class="text-dim" style="font-size:.8rem; text-transform:uppercase; letter-spacing:1px">En cours</div>
                    </div>
                    <div class="card" style="text-align:center; min-width:120px">
                        <div style="font-size:1.5rem; font-weight:900; color:var(--gold)">@_games.Count(g => IsAbandonedStatus(g.Status))</div>
                        <div class="text-dim" style="font-size:.8rem; text-transform:uppercase; letter-spacing:1px">Abandonnées</div>
                    </div>
                    <div class="card" style="text-align:center; min-width:120px">
                        <div style="font-size:1.5rem; font-weight:900; color:var(--success)">@_games.Count(g => g.IsWinner)</div>
                        <div class="text-dim" style="font-size:.8rem; text-transform:uppercase; letter-spacing:1px">Victoires</div>
                    </div>
                </div>

                <div style="overflow-x:auto">
                    <table class="lb-table" aria-label="Historique des parties">
                        <thead>
                            <tr>
                                <th scope="col">Date</th>
                                <th scope="col">Niveau</th>
                                <th scope="col">Mode</th>
                                <th scope="col">Durée</th>
                                <th scope="col">Statut</th>
                                <th scope="col">Résultat</th>
                                <th scope="col"></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var g in _games)
                            {
                                <tr class="@(g.IsWinner ? "me" : "")">
                                    <td>@g.EndedAt.ToString("dd/MM/yy HH:mm")</td>
                                    <td>@g.GameLevel</td>
                                    <td>@g.Queue</td>
                                    <td>@FormatDuration(g.DurationSeconds)</td>
                                    <td><span class="chip chip-muted">@StatusLabel(g.Status)</span></td>
                                    <td>
                                        @if (IsActiveStatus(g.Status))
                                            { <span class="chip chip-accent">En cours</span> }
                                        else if (g.IsWinner)
                                            { <span class="chip chip-success">Victoire 🏆</span> }
                                        else if (IsAbandonedStatus(g.Status))
                                            { <span class="chip chip-muted">Abandonnée</span> }
                                        else
                                            { <span class="chip chip-muted">Défaite</span> }
                                    </td>
                                    <td><a href="/games/@g.GameId" class="btn btn-ghost btn-sm">Voir</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
        </div>
    </div>
}

@code {
    private readonly List<WebGameHistoryItem> _games = [];
    private bool _busy;
    private string? _error;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (!firstRender) return;
        Auth.OnAuthStateChanged += HandleAuthChanged;
        await Auth.InitializeAsync();
        if (!Auth.IsLoggedIn) { StateHasChanged(); return; }
        await LoadAsync();
        StateHasChanged();
    }

    private async void HandleAuthChanged()
    {
        if (!Auth.IsLoggedIn)
        {
            _games.Clear();
            await InvokeAsync(StateHasChanged);
            return;
        }

        if (_busy) return;
        await LoadAsync();
    }

    private async Task LoadAsync()
    {
        _busy = true;
        _error = null;
        try
        {
            var token = await Auth.GetTokenAsync();
            if (token is null) { _error = "Session expirée."; return; }
            _games.Clear();
            _games.AddRange(await Api.GetMyGamesAsync(token));
        }
        catch (Exception ex) { _error = ex.Message; }
        finally { _busy = false; StateHasChanged(); }
    }

    private static string FormatDuration(int s) =>
        s < 60 ? $"{s}s" : s < 3600 ? $"{s / 60}m{s % 60:D2}s" : $"{s / 3600}h{(s % 3600) / 60:D2}m";

    private static bool IsActiveStatus(string status) =>
        string.Equals(status, "active", StringComparison.OrdinalIgnoreCase) ||
        string.Equals(status, "waiting", StringComparison.OrdinalIgnoreCase);

    private static bool IsAbandonedStatus(string status) =>
        string.Equals(status, "abandoned", StringComparison.OrdinalIgnoreCase);

    private static string StatusLabel(string status) => status.ToLowerInvariant() switch
    {
        "active" => "En cours",
        "waiting" => "En attente",
        "abandoned" => "Abandonnée",
        "ended" => "Terminée",
        "finished_normal" => "Terminée",
        _ => status
    };

    public void Dispose()
    {
        Auth.OnAuthStateChanged -= HandleAuthChanged;
    }
}
